 <!-- Navbar -->
<nav class="main-header navbar navbar-expand navbar-white navbar-light">
  <!-- Left navbar links -->
  <ul class="navbar-nav">
    <li class="nav-item">
      <a class="nav-link" data-widget="pushmenu" href="#" role="button"><i class="fas fa-bars"></i></a>
    </li>
    <li class="nav-item d-none d-sm-inline-block">
      <%= link_to "ログアウト", destroy_user_session_path, class:"nav-link", method: :delete %>
    </li>
  </ul>
  <% if current_user.role == 'owner' %>
    <% if current_organization.plan.nil? %>
      <span class="nav-link navbar-danger">プランを選択してください。</span>
    <% elsif current_organization.payment_success == false %>
      <span class="nav-link navbar-danger">支払いが失敗しています。再度プランを選択してください。</span>
    <% end %>
  <% end %>
</nav>
<!-- /.navbar -->